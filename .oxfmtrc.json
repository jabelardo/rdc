{
  "$schema": "./node_modules/oxfmt/configuration_schema.json",

  "//": "Formerly mirrored desktop-plus/.prettierrc.yml (singleQuote/trailingComma es5/no semi/ arrowParens avoid/printWidth 80), to minimize diff noise when rdc's ported code first landed. That trade-off is retired: rdc is greenfield and not racing an MVP date, and the priority is a foundation an outside contributor can pick up with zero setup — which means oxfmt's own defaults, not a bespoke config someone has to learn first. `endOfLine: lf` is kept explicit only because it happens to match the default already and is worth pinning regardless of platform.",

  "endOfLine": "lf",

  "ignorePatterns": [
    ".pnpm-store/**",
    "src-tauri/crates/git-ops/tests/fixtures/**",
    "src/lib/__generated__/**",
    "**/*.md",
    "**/*.toml"
  ]
}
